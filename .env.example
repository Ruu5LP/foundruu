# 必要な環境変数のキーと説明のみを記載しています（値は書かない）。
# ローカルで使う場合は .env にコピーして値を設定してください（.env は .gitignore 済み）。

# `foundruu cloud push` が foundruu-cloud リポジトリへレポートを送信する際に使う
# GitHub トークン。未設定の場合は `gh auth token` にフォールバックします。
# どちらか一方を設定すれば十分です（GH_TOKEN を優先）。
GH_TOKEN=
GITHUB_TOKEN=
