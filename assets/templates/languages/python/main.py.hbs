"""{{projectName}} のエントリポイント（FastAPI アプリ）。"""

from fastapi import FastAPI

app = FastAPI(title="{{projectName}}")


@app.get("/")
def read_root() -> dict[str, str]:
    return {"message": "Hello from {{projectName}}"}
